function ScenarioInput({ scenario, onScenarioChange, category, onCategoryChange, onGenerate, onReset, isLoading }) {
  return <div className="scenario-form"><label htmlFor="category">Select category</label><div className="select-wrap"><select id="category" value={category} onChange={(event) => onCategoryChange(event.target.value)} disabled={isLoading}><option value="Technology">Technology</option><option value="Business">Business</option><option value="Jobs & Workforce">Jobs &amp; Workforce</option><option value="Environment">Environment</option><option value="Society">Society</option><option value="Education">Education</option><option value="Finance">Finance</option><option value="Other">Other</option></select><span>⌄</span></div><label htmlFor="scenario">Describe your decision</label><textarea id="scenario" value={scenario} onChange={(event) => onScenarioChange(event.target.value)} placeholder="e.g. A company replaces 30% of its customer support team with AI chatbots…" disabled={isLoading}/><div className="form-bottom"><span className={scenario.trim().length < 10 ? "count warning" : "count"}>{scenario.length}/500 <small>{scenario.trim().length < 10 ? "Add at least 10 characters" : "Ready to explore"}</small></span><div className="form-actions"><button className="text-button" onClick={onReset} disabled={isLoading}>↻ Reset</button><button className="button button-primary" onClick={onGenerate} disabled={scenario.trim().length < 10 || isLoading}>{isLoading ? "Analyzing…" : "✧ Generate consequences"}<span>→</span></button></div></div></div>;
}
export default ScenarioInput;
